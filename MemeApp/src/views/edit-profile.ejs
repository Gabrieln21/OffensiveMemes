<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0" />
  <title>Edit Profile</title>
  <script src="https://cdn.tailwindcss.com"></script>
</head>
<body class="min-h-screen bg-gradient-to-br from-purple-800 via-fuchsia-900 to-black text-white font-sans">
  <div class="container mx-auto px-6 py-16 max-w-2xl">
    <div class="bg-black/40 border border-white/10 rounded-3xl p-10 shadow-2xl backdrop-blur-md">
      <h1 class="text-4xl font-extrabold text-center bg-gradient-to-r from-purple-300 to-pink-400 bg-clip-text text-transparent mb-10">
        ✏️ Edit Your Profile
      </h1>

      <form method="POST" action="/profile/edit" enctype="multipart/form-data" class="space-y-6">
        <div>
          <label class="block text-purple-200 font-semibold mb-2">Email</label>
          <input name="email" type="email" value="<%= user.email %>" class="w-full bg-gray-900 text-white p-3 rounded-lg border border-purple-600 focus:outline-none focus:ring-2 focus:ring-pink-500" />
        </div>

        <div>
          <label class="block text-purple-200 font-semibold mb-2">New Password</label>
          <input name="password" type="password" class="w-full bg-gray-900 text-white p-3 rounded-lg border border-purple-600 focus:outline-none focus:ring-2 focus:ring-pink-500" />
        </div>

        <div>
          <label class="block text-purple-200 font-semibold mb-2">Winning Message</label>
          <input name="winningMessage" type="text" value="<%= user.winning_message || '' %>" placeholder="e.g. Bow down, peasants!" class="w-full bg-gray-900 text-white p-3 rounded-lg border border-purple-600 focus:outline-none focus:ring-2 focus:ring-pink-500" />
        </div>

        <div>
          <label class="block text-purple-200 font-semibold mb-2">Change Avatar</label>
          <input type="file" name="avatar" accept="image/*" class="w-full bg-gray-900 text-white p-3 rounded-lg border border-purple-600 focus:outline-none" />
        </div>

        <div class="text-right">
          <button type="submit" class="bg-pink-600 hover:bg-pink-700 px-6 py-3 rounded-xl font-bold text-white transition-all duration-300 shadow-lg">
            💾 Save Changes
          </button>
        </div>
      </form>

      <div class="mt-8 text-center">
        <a href="/profile" class="inline-block text-purple-300 hover:text-purple-500 transition duration-200">← Back to Profile</a>
      </div>
    </div>
  </div>
</body>
</html>
